export default function Footer() {
  return (
    <footer className="border-t border-line mt-24">
      <div className="mx-auto max-w-content px-5 sm:px-8 py-10 flex flex-col sm:flex-row gap-4 sm:items-center sm:justify-between text-sm text-inkSoft">
        <p>Calmer is a student project for Melbourne public transport. Sensory scores are estimates, not guarantees.</p>
        <p>Built with curated and mock data - see the admin page for data status.</p>
      </div>
    </footer>
  );
}
